# 기여 안내

식탁보 카탈로그에 기여해주셔서 감사합니다.

기여 방법, 파일 구성, 검증 도구 실행 방법은 [README.md](README.md)에 정리되어 있습니다. 이 문서는 그중에서도 기여하실 때 반드시 지켜주셔야 하는 규범을 따로 모은 것입니다.

## 검증 도구 실행

풀 리퀘스트를 제출하시기 전에 아래 명령으로 스키마 검증과 리소스 생성을 확인해주십시오.

```bash
dotnet run --file src/catalogutil.cs -- ./docs/ ./outputs/
```

## 데이터 수집 시 지켜주실 사항

카탈로그 값을 확인하고 수집하실 때의 원칙은 [docs/DATA_BOUNDARY.md](docs/DATA_BOUNDARY.md)에 정리되어 있습니다. 그중 기여자에게 직접 적용되는 두 가지는 다음과 같습니다.

### 1. 차단을 우회하지 말아주십시오

제공사가 의도한 접근을 차단하거나 배포 정책을 변경한 경우, 그 차단을 우회하는 방식으로 확인한 값을 카탈로그에 반영하지 말아주십시오.

접근이 막혀 값을 확인하기 어려운 경우에는 다음과 같이 처리해주십시오.

- 확인이 불가능하다는 사실을 이슈나 풀 리퀘스트 본문에 적어주십시오.
- 해당 항목의 비활성화나 제거를 제안해주십시오.
- 제공사의 공개 안내 페이지에 정상적인 취득 경로가 안내되어 있다면 그 주소를 알려주십시오.

### 2. 인증 및 유료 게이트 뒤의 리소스는 제외해주십시오

로그인, 결제, 회원 등급 뒤에 있는 리소스는 카탈로그의 대상이 아닙니다. 인증 없이 공개적으로 접근할 수 있는 배포 경로만 기록해주십시오.

대상 서비스의 이용 자체에 로그인이 필요한 것은 무방하지만, 설치 파일을 내려받기 위해 로그인이 필요한 항목은 수록하지 않습니다.

## 출처 메타데이터 기재 (권장)

새로운 항목을 제출하실 때에는 그 값을 무엇을 근거로 언제 확인하셨는지 함께 남겨주시기를 권장합니다. 아래 속성은 모두 선택 사항이며, 비워두셔도 검증은 통과합니다.

| 속성 | 대상 | 내용 |
| --- | --- | --- |
| `SourceUrl` | `Service`, `Package` | 확인의 근거가 된 공식 안내 페이지 주소입니다. |
| `VerifiedOn` | `Service`, `Package` | 확인한 날짜입니다. `YYYY-MM-DD` 형식으로 적어주십시오. |
| `VerifiedBy` | `Service` | 확인하신 분의 식별자입니다. GitHub 핸들을 권장합니다. |

```xml
<Service Id="ExampleBank" DisplayName="예시은행" Category="Banking" Url="https://www.example.com/"
         SourceUrl="https://www.example.com/guide/security" VerifiedOn="2026-08-01" VerifiedBy="octocat">
```

검증 도구는 다음의 경우 Warning을 표시합니다. Warning은 병합을 막지 않습니다.

- `SourceUrl`은 있는데 `VerifiedOn`이 없는 경우
- `VerifiedOn`이 오늘로부터 365일 이상 지난 경우
- `SourceUrl`이 `https`가 아닌 경우

확인하지 못한 값은 추정해서 채우지 말고 비워두십시오. 기존 항목에 대한 소급 입력은 별도로 진행하므로 신규 항목 제출 시에만 신경 쓰시면 됩니다.

## 로고 이미지 기여

로고 이미지는 가능한 한 해당 기관이 공식적으로 배포하는 자산을 사용해주십시오. 표지 사용에 관한 방침은 [TRADEMARKS.md](TRADEMARKS.md)를 참고해주십시오.

## 정정 및 삭제 요청

이미 수록된 항목의 정정이나 삭제를 요청하시는 경우에는 [docs/REMOVAL_REQUEST.md](docs/REMOVAL_REQUEST.md)의 절차를 따라주십시오.
